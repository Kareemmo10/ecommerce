<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>مفاجأة ليكي! ✨</title>
    <style>
        :root {
            --primary-pink: #ff69b4;
            --soft-pink: #ffb6c1;
            --deep-purple: #9b59b6;
            --gold: #ffd700;
        }

        body {
            background-color: #fff0f5;
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            overflow: hidden;
        }

        /* حاوية الصفحات */
        .page {
            display: none; /* بنخفي كله ونظهر واحدة واحدة */
            text-align: center;
            padding: 20px;
            animation: fadeIn 0.8s ease-in-out;
            max-width: 500px;
            width: 90%;
        }

        .active { display: block; }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* استايل الكروت */
        .card {
            background: white;
            padding: 40px;
            border-radius: 30px;
            box-shadow: 0 15px 35px rgba(255, 105, 180, 0.2);
            border: 2px solid var(--soft-pink);
        }

        h1 { color: var(--primary-pink); margin-bottom: 20px; }
        p { color: #555; font-size: 1.2rem; line-height: 1.6; }

        /* الأزرار */
        .btn {
            background: var(--primary-pink);
            color: white;
            border: none;
            padding: 15px 40px;
            border-radius: 50px;
            font-size: 1.1rem;
            cursor: pointer;
            margin-top: 20px;
            transition: 0.3s;
            box-shadow: 0 4px 15px rgba(255, 105, 180, 0.4);
        }

        .btn:hover { transform: scale(1.05); background: #ff1493; }

        /* الـ Photo Frame في الصفحة الأخيرة */
        .photo-frame {
            width: 200px;
            height: 200px;
            margin: 0 auto 20px;
            border-radius: 50%;
            border: 6px solid var(--soft-pink);
            overflow: hidden;
        }
        .photo-frame img { width: 100%; height: 100%; object-fit: cover; }

        /* تلميحات الإثارة */
        .clue { font-size: 4rem; margin-bottom: 20px; display: block; }
    </style>
</head>
<body>

    <!-- الصفحة 1: البداية -->
    <div id="page1" class="page active">
        <div class="card">
            <span class="clue">🤫</span>
            <h1>تفتكري فيه إيه هنا؟</h1>
            <p>جهزي نفسك.. فيه مفاجأة صغيرة مستنية أجمل بنت في الدنيا.</p>
            <button class="btn" onclick="nextPage(2)">أنا مستعدة! 🚀</button>
        </div>
    </div>

    <!-- الصفحة 2: سؤال أو لغز بسيط -->
    <div id="page2" class="page">
        <div class="card">
            <span class="clue">🧐</span>
            <h1>تحدي سريع!</h1>
            <p>قبل ما نشوف المفاجأة.. مين هي أحلى وأطيب بنت أنا عرفتها في حياتي؟</p>
            <input type="text" id="nameInput" placeholder="اكتبي اسمك هنا.." style="padding: 10px; border-radius: 10px; border: 1px solid #ccc; width: 80%; text-align: center;">
            <br>
            <button class="btn" onclick="checkName()">تأكيد 💖</button>
        </div>
    </div>

    <!-- الصفحة 3: العد التنازلي النفسي -->
    <div id="page3" class="page">
        <div class="card">
            <span class="clue">🎁</span>
            <h1>خلاص قربنا..</h1>
            <p>المفاجأة هتبدأ بعد:</p>
            <h2 id="countdown" style="font-size: 4rem; color: var(--primary-pink);">3</h2>
        </div>
    </div>

    <!-- الصفحة 4: الاحتفال النهائي -->
    <div id="page4" class="page">
        <div class="card">
            <header>
                <h1>عيد ميلاد سعيد! 🎂</h1>
            </header>
            <div class="photo-frame">
                <img src="https://via.placeholder.com/200" alt="Birthday Girl">
            </div>
            <p id="personalMsg" class="message">كل سنة وأنتِ طيبة! ✨</p>
            <button class="btn" onclick="launchConfetti()">احتفلي تاني! 🎉</button>
        </div>
    </div>

    <!-- مكتبة الـ Confetti -->
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>

    <script>
        function nextPage(pageNum) {
            // إخفاء كل الصفحات
            document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
            // إظهار الصفحة المطلوبة
            document.getElementById('page' + pageNum).classList.add('active');
        }

        function checkName() {
            const name = document.getElementById('nameInput').value;
            if (name.trim() !== "") {
                document.getElementById('personalMsg').innerText = `كل سنة وأنتِ طيبة يا ${name}! ✨ أتمنى ليكي سنة مليانة سعادة زيك.`;
                startCountdown();
            } else {
                alert("لازم تكتبي اسمك الأول! 😉");
            }
        }

        function startCountdown() {
            nextPage(3);
            let count = 3;
            const timer = setInterval(() => {
                count--;
                if (count > 0) {
                    document.getElementById('countdown').innerText = count;
                } else {
                    clearInterval(timer);
                    nextPage(4);
                    launchConfetti();
                }
            }, 1000);
        }

        function launchConfetti() {
            const duration = 3 * 1000;
            const end = Date.now() + duration;

            (function frame() {
                confetti({
                    particleCount: 5,
                    angle: 60,
                    spread: 55,
                    origin: { x: 0 },
                    colors: ['#ff69b4', '#ffd700']
                });
                confetti({
                    particleCount: 5,
                    angle: 120,
                    spread: 55,
                    origin: { x: 1 },
                    colors: ['#ff69b4', '#ffd700']
                });

                if (Date.now() < end) {
                    requestAnimationFrame(frame);
                }
            }());
        }
    </script>
</body>
</html>